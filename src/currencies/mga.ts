import type { Currency } from "types/types.ts";

/**
 * Malagasy ariary.
 * Rarely use, for testing purposes non decimal currency.
 */
export const MGA: Currency = {
  code: "MGA",
  base: 5,
  exponent: 1,
};
